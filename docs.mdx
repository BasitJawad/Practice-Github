import { Meta } from 'docs'

<Meta
  title="React Component Documentation"
  description="Documentation for the React component displaying a greeting with an age variable."
/>

# React Component Documentation

This documentation provides an overview of a simple React component that displays a greeting along with a user's age.

## Code Overview

The component is written using JavaScript and utilizes the React library. It displays a static message and the age of a user.

```javascript
import React from 'react';

let age = 23;

const index = () => {
  return (
    <div>
      <h1>Hello Basit. Age : {age}</h1>
    </div>
  )
}

export default index;
